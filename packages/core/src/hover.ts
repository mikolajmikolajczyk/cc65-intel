import type { CIndex, HoverInfo } from './types'

// Hover info for the symbol/field/type under `offset`.
//
// SCAFFOLD: resolution lands alongside completion. Returns null.
export function hoverAt(_index: CIndex, _text: string, _offset: number): HoverInfo | null {
  return null
}
